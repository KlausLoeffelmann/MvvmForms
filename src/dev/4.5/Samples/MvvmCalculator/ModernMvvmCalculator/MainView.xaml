<Page
    x:Class="ModernMvvmCalculator.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ModernMvvmCalculator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="6*"/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="History" HorizontalAlignment="Left" Margin="5,5,10,5" Grid.RowSpan="2"/>
        <ListBox Grid.Row="0" x:Name="listBox" Margin="5,30,5,5" />
        <TextBlock x:Name="textBox" Grid.Row="1" 
                 TextWrapping="Wrap" 
                 Text="TextBox" 
                 FontFamily="Consolas" 
                 FontSize="24" 
                 TextAlignment="Right" 
                 VerticalAlignment="Center"/>

    </Grid>
</Page>
